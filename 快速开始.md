# 快速开始

> 装好 CNplus 和 VSCode 扩展，在编辑器里写出第一个程序。全程约 10 分钟，不需要任何编程基础。

## 一、装 CNplus 本体

CNplus 跑在 Python 上，所以先要有 **Python 3.11 或更新版本**（[python.org](https://www.python.org/downloads/) 下载，一路「下一步」装完即可）。

装好 Python 后，打开终端（macOS 是「终端」App，Windows 是 PowerShell），依次执行：

```bash
git clone https://github.com/CNplus/CNplus-lang.git
cd CNplus-lang
python3 -m venv .venv && source .venv/bin/activate
pip install -e ".[lsp]"
```

最后一步会花一两分钟。`[lsp]` 是给编辑器扩展用的，一次装齐，后面省事。

装完先验证一下：

```bash
python -m cnplus 运行 示例/01-你好.cnp
```

屏幕输出 `你好，世界！` 就说明装好了。

> 也可以用 `cnp` 命令代替 `python -m cnplus`，两者等价：`cnp 运行 示例/01-你好.cnp`。

## 二、装 VSCode 扩展

装好本体，再来装编辑器扩展——它负责让 `.cnp` 文件有**语法高亮、实时中文报错、按 F5 直接运行**。

**1. 下载**：从 [GitHub Release](https://github.com/CNplus/CNplus-lang/releases) 下载最新的 `cnplus-<版本>.vsix` 文件。

**2. 安装**（两种方式任选）：

- **图形界面（推荐，不用装任何东西）**：在 VSCode 里按 `⌘⇧X`（Windows/Linux 是 `Ctrl+Shift+X`）打开扩展面板，点右上角 `…` → 「从 VSIX 安装…」，选中下载的 `.vsix` 文件。
- **命令行**：`code --install-extension cnplus-0.8.0.vsix`（需先装好 `code` 命令：按 `⌘⇧P` 输入 `install code`，选「Shell Command: Install 'code' command in PATH」，重开终端再试）。

**3. 重启**：完全退出 VSCode 再打开（⌘Q，不是关窗口），扩展才会生效。

> **电脑上装了多个 Python？** 扩展会自动找一个装了 CNplus 的来用，不用手动配置。想知道用的是哪个，`⌘⇧P` 运行「CNplus: 显示当前使用的解释器」。
>
> 注意：不要用 `ln -s` 把扩展目录链接进 `~/.vscode/extensions/`——新版 VSCode 不再加载这种方式装的扩展，`.cnp` 文件会一直显示「纯文本」。

## 三、在 VSCode 里写第一个程序

**1. 打开 VSCode**，新建一个文件夹存放你的程序（菜单「文件 → 打开文件夹」，随便起个名，比如 `我的程序`）。

**2. 新建文件**：在左侧资源管理器空白处右键 → 「新建文件」，命名为 `你好.cnp`（扩展名必须是 `.cnp`）。

**3. 写代码**：粘贴下面两行——

```cnp
设 名字 = "小明"
打印(@"你好，{名字}")
```

**4. 运行**：按 `F5`（或点右上角 ▶ 按钮）。下方终端会输出：

```
你好，小明
```

看到这行字，你就已经会用 CNplus 写程序了。刚才写的两行是 CNplus 的「变量」和「格式串」——具体是什么意思，[教程](教程/01-变量与类型.md)第一章会讲。现在先把环境跑通，感受一下。

## 四、命令行速查

平时在 VSCode 里按 F5 就够用。想直接在终端里跑，这些命令备查：

| 命令 | 作用 |
|---|---|
| `cnp 运行 x.cnp` | 直接解释执行 |
| `cnp 运行 --python x.cnp` | 转译成 Python 再执行（能借用 pip 库） |
| `cnp 编译 x.cnp` | 生成独立的 `.py` 文件，可到处运行 |
| `cnp 检查 x.cnp` | 只查错，不执行 |
| `cnp 词法 x.cnp` | 打印词元流（调试用） |
| `cnp 语法树 x.cnp` | 打印语法树（调试用） |
| `cnp 版本` | 显示版本 |

---

接下来：[教程 · 一、变量与类型](教程/01-变量与类型.md)
