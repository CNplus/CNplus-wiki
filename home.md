---
title: CNplus
description: CNplus 中文编程语言项目首页
published: true
editor: markdown
---

# CNplus —— 使用中文撸代码

> 一门中文编程语言。玩具起步，架构上留有升级成正式语言的出口。

```cnp
设 单价 = 15
设 数量 = 4

打印("总价：" + 文本(单价 * 数量) + " 元")

如果 单价 * 数量 > 50
    打印("满 50 包邮！")
否则
    打印("再买点就包邮了")
```

**没写过程序？** 从[开始](教程/00-开始.md)出发——序言带你认识它，然后挑一条路写出第一行代码。
**写过 Python / JS？** 直接看[与 Python 的差异](参考/与Python的差异.md)，一分钟定位区别。

---

## 这是什么

CNplus 让你用中文关键字写程序。它**不是** Python 的「中文函数名封装」，而是有**自己的**词法器（lexer）、语法分析器（parser）和抽象语法树（AST）的独立语言，后端可插拔。

| 特点 | 说明 |
|---|---|
| 🌏 全中文 | 关键字、类型名、错误信息、诊断码全部中文 |
| 🔌 可插拔后端 | 同一份源码，既能直接解释执行，也能转译成 Python |
| 🧱 严格语义 | 刻意和 Python 拉开距离，新手头号坑源被堵死 |
| 💬 三层报错 | 术语 + 大白话 + 能直接照抄的改法 |
| 🗣️ 一意多写 | `设`=`令`=`让`，用顺口的那个 |

### 一个意思，多种写法

用你顺口的那个，效果完全一样。

**关键字白话别名**（每组第一个是推荐写法）：

| 意思 | 可以写成 |
|---|---|
| 建立名字 | `设` `令` `让` |
| 判断 | `如果` `若` `假如` |
| 否则 | `否则` `不然` `要不然` |
| 重复 | `循环` `当` `重复` |
| 造工具 | `函数` `定义` `过程` |
| 给出结果 | `返回` `返` `给出` |

**成员方法（点调用）**——同一个内置函数，两种写法：

```cnp
名单.包含("小明")    # 读作「名单：包含小明吗」
包含(名单, "小明")    # 读作「名单里有没有小明」
```

列表、字典、字符串上支持点调用的内置函数都能这样点调用。用类的方法（`学生.名片()`）也是这一套，心智模型统一。

**遍历的两种语序**：

```cnp
设 成绩 = {"小明": 85, "小红": 92}

遍历 成绩 每个 名字    # 自然中文：集合在前、名字在后
    打印(名字)

对于 名字 在 成绩      # 英文习惯：照 for x in y 语序
    打印(名字)
```

**取反的两种写法**：`不 年龄 >= 18` 和 `非 年龄 >= 18` 等价——`不` 是中文母语者脱口而出的写法。

完整清单见[关键字与别名](参考/关键字与别名.md)。

---

## 为什么要另做一门语言

Python 强大，但对新手有三处「隐式魔法」是坑：`if 0` 能跑但含义微妙、`1 == "1"` 悄悄返回假、变量没声明也能等到运行时才炸。CNplus 的选择是**当场提醒**：

| 主题 | CNplus | Python |
|---|---|---|
| 真值 | 只有 `真`/`假` 能出现在条件位置，`如果 0` 是编译错误 | 隐式真值 |
| 跨类型比较 | 数 vs 串比较报错 | 返回 `False` |
| 未声明变量 | 编译期错误 | 运行时 `NameError` |

---

## 架构：玩具与正式语言的分界线

```
源码 .cnp
  → lexer（词法）→ parser（AST）→ checker（静态检查）→ 后端（执行）
```

CNplus 有**自己的** lexer、parser 和 AST，后端可插拔。三个后端行为逐字一致：

| 后端 | 命令 | 特点 |
|---|---|---|
| 树遍历 | `cnp 运行 x.cnp` | 直接解释执行，启动快，报错精准 |
| Python 转译 | `cnp 运行 --python x.cnp` | 先翻成 Python 再跑，能借用 pip 生态 |
| JS 转译 | `cnp 运行 --js x.cnp` / `cnp 编译 x.cnp --js` | 直接跑需 Node.js；编译生成独立 `.js`，node 或浏览器都能跑 |

「源码 → AST」与「AST → 执行」之间的这条接缝，让**加一个后端就是加文件、不用改架构**——JS 后端的落地就是证明。

---

## 开始学习

从[开始](教程/00-开始.md)出发：序言讲清它的历史和特性，然后带你挑一条路（VSCode / 网页 / 命令行）写出第一个程序。零基础 10 分钟上手，全程图文。

想先不动手、直接在浏览器里玩？打开[在线运行器](https://cnplus.org/playground)，零安装。

---

## 项目状态

- **版本**：v1.4.4（2026-08）
- **测试**：928 passed + 5 skipped，三个后端逐字对拍
- **示例**：18 个可运行程序，由易到难
- **路线**：二维列表算法 → 字节码 VM → 模块多文件

## 文档导航

| 文档 | 给谁看 |
|---|---|
| [开始](教程/00-开始.md) | 序言 + 安装环境 + 第一个程序 |
| [示例索引](示例索引.md) | 18 个示例程序，学完一章跑一个 |

**教程**（按顺序读：序言 + 五章，共 15 个课时）：

| 篇 | 入口 |
|---|---|
| 开始（序言） | [开始](教程/00-开始.md) |
| 第一章 起步 | [变量与类型](教程/01-起步/01-变量与类型/01-教程.md) → 做判断 → 重复做 |
| 第二章 造工具 | [函数](教程/02-造工具/01-函数/01-教程1.md) |
| 第三章 装数据 | [列表](教程/03-装数据/01-列表/01-教程1.md) → 字典 → 遍历 |
| 第四章 让程序活起来 | [输入输出](教程/04-让程序活起来/01-输入输出/01-教程.md) → 内置函数 → 借库 → 错误 → 异常 |
| 第五章 面向对象 | [类入门](教程/05-面向对象/01-类入门/01-教程.md) → 类进阶 → 类实践 |

**参考**：

| 文档 | 给谁看 |
|---|---|
| [关键字与别名](参考/关键字与别名.md) | 全部关键字 + 别名 + 成员方法 + 遍历语序 |
| [诊断码表](参考/诊断码.md) | 查错误编号 |
| [运行程序的几种方式](参考/运行程序的几种方式.md) | REPL、JS 后端、在线运行器等全部跑法 |
| [与 Python 的差异](参考/与Python的差异.md) | 有基础的人一分钟定位区别 |

## 历史

CNplus 始于 2021 年 12 月，最初是一层中文函数名封装；2022 年 6 月尝试用 C++ 重做成 IDLE，停在 `main.cpp:28` 一个没写完的 `cout` 上。2026 年重启，这次先建「源码 → AST」与「AST → 执行」之间的接缝，再建楼。老仓库原样保存在 `archive/` 下作只读参考。

## 许可

Apache-2.0，延续 2021 年原项目的选择。
