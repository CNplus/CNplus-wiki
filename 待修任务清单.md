# Wiki 待修任务清单

> 面向维护 CNplus-wiki 的协作者。本清单由主仓库（CNplus-lang）一侧核对后生成，
> 逐条给出精确位置与改法。按「必修 → 建议」优先级排列。
>
> 核对基准：主仓库最新已发布版本 **v0.7.2**，当前 **441 个测试全绿**。
> 生成时间：2026-08-22。

---

## 总体结论

Wiki 内容完整度与质量都很好。v0.5–v0.7 的语言特性（否则如果、默认参数、
自增、复合赋值、成员方法、格式串、询问/询问数值、多返回值解包、异常处理、
索引复合赋值）**均已覆盖**，语法写法也正确（未发现使用已废除的旧写法）。

需要处理的只有以下三类，其中**只有第一类（版本号）是必修**：
第二类是为未来自动校验预留的改进建议，第三类已核对完毕、无需改动。

---

## 一、必修：版本号与测试数过时

主仓库已推进到 v0.7.2 / 441 测试，Wiki 仍停留在 v0.7.1 / 424。

| 文件 | 行 | 现状 | 改为 |
|---|---|---|---|
| `home.md` | 104 | `- **版本**：v0.7.1（2026-08）` | `- **版本**：v0.7.2（2026-08）` |
| `home.md` | 105 | `- **测试**：424 个全绿，两个后端逐字对拍` | `- **测试**：441 个全绿，两个后端逐字对拍` |
| `教程/10-借用Python库.md` | 13 | `……靠 424 个测试里的「对拍」保证的` | `……靠 441 个测试里的「对拍」保证的` |

注：`17 个示例` 的说法是正确的，不需要改。

v0.7.2 是文档规范整理版本，未新增语言特性，因此教程正文无需补写新语法，
只需同步上述数字。

---

## 二、建议：给「故意报错」的示例加校验豁免标记

Wiki 中有若干代码块是**有意演示错误**的（注释里已写明错误码，教学意图正确）。
若将来对 Wiki 做「所有代码块自动运行」的 CI 校验，这些块会被误判为失败。
建议在这些块前统一加一行注释标记（例如 `<!-- 故意报错，勿校验 -->`），
便于未来自动化区分「应跑通」与「应报错」。

涉及的块：

| 文件 | 演示内容 |
|---|---|
| `教程/01-变量与类型.md` | 重复声明报 CN0307 |
| `教程/02-做判断.md` | 条件非布尔报 CN0301 |
| `教程/05-列表.md` | 下标越界报 CN0308 |
| `教程/07-遍历.md` | 整数不可遍历报 CN0309 |
| `教程/11-看懂错误.md` | 静态 vs 运行时报错对比（两块） |

这不是错误，是为未来的自动校验预留区分能力。标记形式可由维护者自定，
只要能被 CI 脚本识别即可。

---

## 三、已核对，无需处理：指向主仓库 docs/ 的链接

主仓库已删除 5 篇与 Wiki 重复的用户教程（十分钟上手、列表和字典、输入输出、
异常处理、借用 Python 库），教学内容现以 **Wiki 为唯一来源**。

已在 Wiki 全仓扫描全部 markdown 链接，结果：Wiki **未引用**主仓库
`docs/` 下的任何文件，仅 `README.md` 链接到主仓库首页
（`https://github.com/CNplus/CNplus-lang`），该链接有效。

**此项无需改动**，列出仅为说明主仓库变更未影响 Wiki。

---

## 附：主仓库这一侧已完成的事（供背景参考）

- 删除 5 篇与 Wiki 重复的用户教程，README 文档区改为指向 wiki.cnplus.org
- 主仓库 `docs/` 现只保留面向实现者/贡献者的规范：
  `spec/语义约定`、`spec/诊断码`、`贡献/release说明规范`、`decisions/`
- 全部 17 个 GitHub Release 说明已按统一规范重写（第三人称、配可运行示例）

分工现已明确：**Wiki = 面向真实用户的教学；主仓库 = 面向开发者与实现者的规范。**
今后 Wiki 只需跟随主仓库的已发布版本同步新特性与版本号即可。
