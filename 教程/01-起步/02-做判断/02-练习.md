# 练习 · 做判断

> 学完「做判断」，动手做一做。做完点开参考答案对一对。

---

## 练习

写一个程序：判断一个数是不是偶数。

提示：偶数除以 2 的余数是 0，取余用 `%`。

<details>
<summary>参考答案</summary>

```cnp
设 数 = 8

如果 数 % 2 == 0
    打印(文本(数) + " 是偶数")
否则
    打印(文本(数) + " 是奇数")
```

> **解析**：`数 % 2` 是「数除以 2 的余数」。偶数除以 2 余 0，所以 `数 % 2 == 0` 成立就是偶数。注意是**两个**等号 `==`（判断相等），不是 `=`（赋值）。`文本(数)` 把数字转成文字才能拼接。

</details>

---

下一课：[重复做](../03-重复做/01-教程.md)
